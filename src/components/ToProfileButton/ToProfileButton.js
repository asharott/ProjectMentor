import React from "react";

const ToProfileButton = (props) => {
  return (
    <div className="toProfileButtonContainer">
      <button type="submit" className="toProfileButton">
        Voir mon profil
      </button>
    </div>
  );
};

export default ToProfileButton;
